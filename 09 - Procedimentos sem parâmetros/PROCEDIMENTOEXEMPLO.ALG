Algoritmo "procedimentoExemplo"

procedimento soma
var aux: inteiro
inicio
      aux <- n + m
      res <- aux
fimprocedimento

Var

res, m, n: inteiro

Inicio

      n <- 4
      m <- -9
      soma
      escreva (res)

Fimalgoritmo